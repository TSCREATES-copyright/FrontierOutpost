export function createPlayerSystem(context) {
  return {
    update(dt) {
      // Player logic is currently hosted in systems/game.js for compatibility.
      void dt;
      void context;
    },
  };
}
