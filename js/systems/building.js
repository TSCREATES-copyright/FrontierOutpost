export function createBuildingSystem(context) {
  return {
    update(dt) {
      // Building logic remains in systems/game.js in this compatibility phase.
      void dt;
      void context;
    },
  };
}
